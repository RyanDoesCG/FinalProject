

    PROJECT CONTRACT

    CREATE SEEDING SYSTEM

 1  RENDER AND LIGHT ROTATING SPHERE WITH ITS SURFACE AREA DEVIDED EVENLY INTO ITS
    GENERATED NUMBER OF BIOMES

 2  WRITE INPUT HANDLING CODE FOR MOUSE, KEYBOARD AND GAMEPAD CONTROLLING THE ROTATION
    OF THE SPHERE, ZOOMING IN AND OUT AND SELECTING BIOMES.

 3  WRITE A SHADER PROGRAM FOR EACH BIOME TEXTURE

 4  DISTORT PLANET MESH WITH NOISE FOR TERRAIN


glm::ortho(0.0f, 800.0f, 0.0f, 600.0f, 0.1f, 100.0f);

glm::mat4 proj = glm::perspective(45.0f, (float)width/(float)height, 0.1f,
100.0f);



/*
IDEAS/TO-DO

Moon biome?
Space stations?
Custom RNG?

NAMES
Domhan      (world in gaelic)
PolyTerra   (many worlds in latin)

voidstar

*/


// Sphere/Planet: http://stackoverflow.com/questions/7687148/drawing-sphere-in-opengl-without-using-glusphere
// Sky/Terrain:   http://vterrain.org/Atmosphere/


//    if      (action == GLFW_PRESS)   activeKeys[key] = true;
//    else if (action == GLFW_RELEASE) activeKeys[key] = false;

/*
if (action == GLFW_PRESS) {
if (button == GLFW_MOUSE_BUTTON_RIGHT) {
mouseEvents[MOUSE_RIGHT_CLICK] = true;
}

else if (button == GLFW_MOUSE_BUTTON_LEFT) {
mouseEvents[MOUSE_LEFT_CLICK] = true;
}
}

else if (action == GLFW_RELEASE) {
if (button == GLFW_MOUSE_BUTTON_RIGHT) {
mouseEvents[MOUSE_RIGHT_CLICK] = false;
}

else if (button == GLFW_MOUSE_BUTTON_LEFT) {
mouseEvents[MOUSE_LEFT_CLICK] = false;
}
}
*/
